<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Ueditor.Controls.TerminalPane"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
          BorderThickness="0,1,0,0"
          Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="180"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.ColumnSpan="2" Padding="8,5,8,5" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="TerminalTitleText"
                       Text="터미널"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
            <Button Grid.Column="1" Content="새 터미널" Click="OnNewTerminalClick" Padding="10,3,10,3"/>
            <Button Grid.Column="2" Content="닫기" Click="OnCloseTerminalClick" Padding="10,3,10,3"/>
        </Grid>

        <Border x:Name="TerminalHostBorder"
                Grid.Row="1"
                Grid.Column="0"
                Background="Black"
                Visibility="Collapsed"
                SizeChanged="OnTerminalHostBorderSizeChanged"
                PointerPressed="OnTerminalHostBorderPointerPressed"/>

        <TextBox x:Name="TerminalOutputTextBox"
                 Grid.Row="1"
                 Grid.Column="0"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="NoWrap"
                 FontFamily="Consolas"
                 FontSize="12"
                 BorderThickness="0"
                 Background="{ThemeResource SystemControlBackgroundAltHighBrush}"
                 Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"/>

        <Grid x:Name="TerminalInputAreaGrid" Grid.Row="2" Grid.Column="0" Padding="8" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="PS>"
                       VerticalAlignment="Center"
                       FontFamily="Consolas"
                       FontWeight="SemiBold"
                       Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
            <TextBox x:Name="TerminalInputBox"
                     Grid.Column="1"
                     PlaceholderText="명령 입력 후 Enter"
                     FontFamily="Consolas"
                     Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                     KeyDown="OnTerminalInputKeyDown"/>
        </Grid>

        <Border Grid.Row="1"
                Grid.Column="1"
                Grid.RowSpan="2"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                BorderThickness="1,0,0,0"
                Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
            <ListView x:Name="TerminalSessionsList"
                      SelectionMode="Single"
                      SelectionChanged="OnTerminalSessionsListSelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8,6,8,6">
                            <TextBlock Text="{Binding DisplayTitle}" TextTrimming="CharacterEllipsis"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</UserControl>
